'use strict';

var app = angular.module('app', []);

app.config([function(){
    
}]);

app.run([function(){
    
}]);

app.controller('mainCtrl', ['$log', function ($log) {
    $log.debug('Running main controller');
    
    var vm = this;
}])